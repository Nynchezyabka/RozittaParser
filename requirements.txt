# -*- coding: utf-8 -*-
# ── Основные зависимости ──────────────────────────────────────────────────────
PySide6>=6.6.0
Telethon>=1.35.0
python-docx>=1.1.0
python-dotenv>=1.2.2

# ── База знаний для ИИ (пресет #96) ──────────────────────────────────────────
# python-frontmatter: идемпотентная работа с YAML front-matter в Markdown.
# Pure-Python, транзитивно тянет только PyYAML (уже нужен Telethon).
python-frontmatter>=1.0.0

# ── Ускорение шифрования Telethon ─────────────────────────────────────────────
# cryptg даёт 10-20x прирост скорости MTProto — рекомендуется
cryptg
pyaes

# ── Прокси (опционально) ──────────────────────────────────────────────────────
# Раскомментируйте если используете SOCKS5/MTProto прокси:
python-socks

# ── Расширенные фильтры сообщений (опционально) ───────────────────────────────
# Раскомментируйте для поддержки expression-фильтров (P2-2 в roadmap):
simpleeval

# ── Распознавание речи STT (опционально, тяжёлая зависимость ~500 МБ) ─────────
# Раскомментируйте для расшифровки голосовых и кружочков:
faster-whisper

# ── Импорт сессии из Telegram Desktop (опционально) ───────────────────────────
# Раскомментируйте для кнопки «Импорт из Telegram Desktop»:
opentele2
tgcrypto>=1.2.5

# ── Компонент «Распознавание изображений» (component_vlm/) ───────────────────
# Pillow уменьшает картинки перед подачей модели. Само приложение его не
# импортирует: компонент собирается отдельно и живёт своим окружением, но
# тесты компонента идут в общем прогоне, поэтому пакет нужен и здесь.
pillow>=10.0.0

# ── Сборка .exe (только для разработчиков) ────────────────────────────────────
pyinstaller>=6.0.0

# ── Тестирование и покрытие ─────────────────────────────────────────────────────
pytest
pytest-asyncio
pytest-cov
pytest-timeout
