"""
component_vlm/entry.py — точка входа собранного компонента.

Отдельный файл, а не запуск worker.py напрямую: PyInstaller замораживает
модуль, и относительные импорты внутри пакета в замороженном виде ведут
себя иначе, чем при запуске из репозитория. Крошечная обёртка снимает
вопрос — пакет импортируется как пакет в обоих случаях.
"""

import sys

from component_vlm.worker import main

if __name__ == "__main__":
    sys.exit(main())
